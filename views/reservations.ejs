<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reservations</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        .reservation-info {
            font-size: 20px;
            margin-bottom: 10px; 
        }
    </style>
</head>
<body>
    <div class="px-4 py-5 my-5 text-center">
        <div class="col-lg-6 mx-auto">
            <% if (reservations.length === 0) { %>
                <h1>No reservations found.</h1>
            <% } else { %>
                <% reservations.forEach(reservation => { %>
                    <div class="reservation-info">
                        <h2>Reservation at: </h2>
                        <p>Hotel Name: <%= reservation.hotel_name %></p>
                        <p>Room type: 
                            <% if (reservation.room_type === 1) { %>
                                Single Room
                            <% } else if (reservation.room_type === 2) { %>
                                Double Room
                            <% } else if (reservation.room_type === 3) { %>
                                Suite
                            <% } %>
                        </p>
                        <p>Room price: <%= reservation.room_price %></p>
                        <p>Check-in Date: <%= reservation.date_check_in %></p>
                        <p>Check-out Date: <%= reservation.date_check_out %></p>
                        <p>Check-in Hour: <%= reservation.hour_check_in %></p>
                        <form action="/cancel-reservation" method="GET">
                            <input type="hidden" name="reservationId" value="<%= reservation.id %>">
                            <input type="hidden" name="reservationCheckInDate" value="<%= reservation.date_check_in %>">
                            <input type="hidden" name="reservationCheckOutDate" value="<%= reservation.date_check_in %>">
                            <input type="hidden" name="reservationCheckInHour" value="<%= reservation.hour_check_in %>">
                            <button type="submit" class="btn btn-danger">Cancel Reservation</button>
                        </form>
                    </div>
                    <hr>
                <% }); %>
            <% } %>
        </div>
    </div>
</body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</html>
