<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rooms Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        .room-info {
            font-size: 24px;
            margin-bottom: 20px; 
            text-align: left; 
        }
        .room-info img {
            max-width: 300px;
            height: auto;
            margin-right: 20px; 
        }
    </style>
</head>
<body>
    <div class="px-4 py-5 my-5">
        <div class="container">
            <% if (rooms.length === 0) { %>
                <h1 class="text-center">No rooms available for this hotel</h1>
            <% } else { %>
                <% rooms.forEach(room => { %>
                    <div class="room-info">
                        <% if (room.is_available == true){ %>
                            <% if (room.type === 1) { %>
                                <img src="https://webbox.imgix.net/images/owvecfmxulwbfvxm/c56a0c0d-8454-431a-9b3e-f420c72e82e3.jpg" alt="Single Room Image" class="img-fluid mb-3">
                                Single Room - <%= room.price %> RON
                            <% } else if (room.type === 2) { %>
                                <img src="https://www.londonhousehotels.com/wp-content/uploads/2017/11/5.-1-standard-double-MAIN.jpg" alt="Double Room Image" class="img-fluid mb-3">
                                Double Room - <%= room.price %> RON
                            <% } else if (room.type === 3) { %>
                                <img src="https://i1.wp.com/www.differencebetween.com/wp-content/uploads/2016/01/Difference_Between_Suite_Room_Image1.jpg" alt="Suite Image" class="img-fluid mb-3">
                                Suite - <%= room.price %> RON
                            <% } %>
                            <form action="/make-reservation" method="GET">
                                <input type="hidden" name="roomId" value="<%= room.id %>">
                                <input type="hidden" name="hotelId" value="<%= room.hotel_id %>">
                                <button class="btn btn-primary" type="submit">Make Reservation</button>
                            </form>
                        <% } %>
                    </div>
                <% }); %>
            <% } %>
        </div>
    </div>
</body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</html>
